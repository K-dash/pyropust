# Auto-generated by tools/gen_native_stub.py
# Do not edit manually! Edit pyropust/__init__.pyi instead.
#
# This file provides type information for the native Rust module.
# For the public API, see pyropust/__init__.pyi.

from collections.abc import Callable, Mapping
from enum import StrEnum
from typing import Generic, Never, TypeVar

class ErrorCode(StrEnum): ...  # type: ignore[misc]

# Variance-sensitive type parameters require old-style TypeVar
T_co = TypeVar("T_co", covariant=True)

class Result(Generic[T_co]):
    def is_ok(self) -> bool: ...
    def is_err(self) -> bool: ...
    def is_ok_and(self, predicate: Callable[[T_co], object]) -> bool: ...
    def is_err_and(self, predicate: Callable[[Error], object]) -> bool: ...
    def unwrap(self) -> T_co: ...
    def unwrap_err(self) -> Error: ...
    def expect(self, msg: str) -> T_co: ...
    def expect_err(self, msg: str) -> Error: ...
    def unwrap_or[U](self, default: U) -> T_co | U: ...
    def unwrap_or_else[U](self, f: Callable[[Error], U]) -> T_co | U: ...
    def ok(self) -> Option[T_co]: ...
    def err(self) -> Option[Error]: ...
    def map[U](self, f: Callable[[T_co], U]) -> Result[U]: ...
    def map_try[U](
        self,
        f: Callable[[T_co], U],
        *,
        code: str | ErrorCode,
        message: str,
        kind: ErrorKind | str | None = None,
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
    ) -> Result[U]: ...
    def and_then_try[U](
        self,
        f: Callable[[T_co], Result[U]],
        *,
        code: str | ErrorCode,
        message: str,
        kind: ErrorKind | str | None = None,
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
    ) -> Result[U]: ...
    def map_err(self, f: Callable[[Error], Error]) -> Result[T_co]: ...
    def map_or[U](self, default: U, f: Callable[[T_co], U]) -> U: ...
    def map_or_else[U](self, default_f: Callable[[Error], U], f: Callable[[T_co], U]) -> U: ...
    def inspect(self, f: Callable[[T_co], object]) -> Result[T_co]: ...
    def inspect_err(self, f: Callable[[Error], object]) -> Result[T_co]: ...
    def context(
        self,
        message: str,
        *,
        code: str | ErrorCode = "context",
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
    ) -> Result[T_co]: ...
    def with_code(self, code: str | ErrorCode) -> Result[T_co]: ...
    def map_err_code(self, prefix: str) -> Result[T_co]: ...
    def and_[U](self, other: Result[U]) -> Result[U]: ...
    def or_(self, other: Result[T_co]) -> Result[T_co]: ...
    def or_else(self, f: Callable[[Error], Result[T_co]]) -> Result[T_co]: ...
    def and_then[U](self, f: Callable[[T_co], Result[U]]) -> Result[U]: ...
    def flatten[T](self: Result[Result[T]]) -> Result[T]: ...
    def transpose[T](self: Result[Option[T]]) -> Option[Result[T]]: ...
    def unwrap_or_raise(self, exc: BaseException) -> T_co: ...
    @classmethod
    def attempt[T](cls, f: Callable[[], T], *exceptions: type[BaseException]) -> Result[T]: ...

class Option(Generic[T_co]):
    def is_some(self) -> bool: ...
    def is_none(self) -> bool: ...
    def is_some_and(self, predicate: Callable[[T_co], object]) -> bool: ...
    def is_none_or(self, predicate: Callable[[T_co], object]) -> bool: ...
    def unwrap(self) -> T_co: ...
    def expect(self, msg: str) -> T_co: ...
    def unwrap_or[U](self, default: U) -> T_co | U: ...
    def unwrap_or_else[U](self, f: Callable[[], U]) -> T_co | U: ...
    def map[U](self, f: Callable[[T_co], U]) -> Option[U]: ...
    def map_try[U](
        self,
        f: Callable[[T_co], U],
        *,
        code: str | ErrorCode,
        message: str,
        kind: ErrorKind | str | None = None,
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
    ) -> Result[Option[U]]: ...
    def map_or[U](self, default: U, f: Callable[[T_co], U]) -> U: ...
    def map_or_else[U](self, default_f: Callable[[], U], f: Callable[[T_co], U]) -> U: ...
    def inspect(self, f: Callable[[T_co], object]) -> Option[T_co]: ...
    def filter(self, predicate: Callable[[T_co], object]) -> Option[T_co]: ...
    def and_[U](self, other: Option[U]) -> Option[U]: ...
    def and_then[U](self, f: Callable[[T_co], Option[U]]) -> Option[U]: ...
    def and_then_try[U](
        self,
        f: Callable[[T_co], Option[U]],
        *,
        code: str | ErrorCode,
        message: str,
        kind: ErrorKind | str | None = None,
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
    ) -> Result[Option[U]]: ...
    def or_(self, other: Option[T_co]) -> Option[T_co]: ...
    def or_else(self, f: Callable[[], Option[T_co]]) -> Option[T_co]: ...
    def xor(self, other: Option[T_co]) -> Option[T_co]: ...
    def flatten[T](self: Option[Option[T]]) -> Option[T]: ...
    def transpose[T](self: Option[Result[T]]) -> Result[Option[T]]: ...
    def zip[U](self, other: Option[U]) -> Option[tuple[T_co, U]]: ...
    def zip_with[U, R](self, other: Option[U], f: Callable[[T_co, U], R]) -> Option[R]: ...
    def ok_or(
        self,
        code: str | ErrorCode,
        message: str,
        *,
        kind: ErrorKind | str | None = None,
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
        cause: str | None = None,
    ) -> Result[T_co]: ...
    def ok_or_else(
        self,
        code: str | ErrorCode,
        f: Callable[[], str | Error],
        *,
        kind: ErrorKind | str | None = None,
        metadata: Mapping[str, str] | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
        cause: str | None = None,
    ) -> Result[T_co]: ...

class ErrorKind:
    InvalidInput: ErrorKind
    NotFound: ErrorKind
    Internal: ErrorKind

class Error:
    @property
    def kind(self) -> ErrorKind: ...
    @property
    def code(self) -> str: ...
    @property
    def message(self) -> str: ...
    @property
    def metadata(self) -> dict[str, str]: ...
    @property
    def op(self) -> str | None: ...
    @property
    def path(self) -> list[str | int]: ...
    @property
    def expected(self) -> str | None: ...
    @property
    def got(self) -> str | None: ...
    @property
    def cause(self) -> str | None: ...
    def to_dict(self) -> dict[str, object]: ...
    @classmethod
    def from_dict(cls, data: dict[str, object]) -> Error: ...
    @classmethod
    def new(
        cls,
        code: str | ErrorCode,
        message: str,
        *,
        kind: ErrorKind | str | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
        metadata: Mapping[str, str] | None = None,
    ) -> Error: ...
    @classmethod
    def wrap(
        cls,
        err: BaseException | Error,
        *,
        code: str | ErrorCode,
        message: str,
        kind: ErrorKind | str | None = None,
        op: str | None = None,
        path: list[str | int] | None = None,
        expected: str | None = None,
        got: str | None = None,
        metadata: Mapping[str, str] | None = None,
    ) -> Error: ...

def Ok[T](value: T) -> Result[T]: ...
def Err(error: Error) -> Result[Never]: ...
def err(
    code: str | ErrorCode,
    message: str,
    *,
    kind: ErrorKind | str | None = None,
    metadata: Mapping[str, str] | None = None,
    op: str | None = None,
    path: list[str | int] | None = None,
    expected: str | None = None,
    got: str | None = None,
    cause: str | None = None,
) -> Result[Never]: ...
def bail(
    code: str | ErrorCode,
    message: str,
    *,
    kind: ErrorKind | str | None = None,
    metadata: Mapping[str, str] | None = None,
    op: str | None = None,
    path: list[str | int] | None = None,
    expected: str | None = None,
    got: str | None = None,
    cause: str | None = None,
) -> Result[Never]: ...
def ensure(
    condition: object,
    code: str | ErrorCode,
    message: str,
    *,
    kind: ErrorKind | str | None = None,
    metadata: Mapping[str, str] | None = None,
    op: str | None = None,
    path: list[str | int] | None = None,
    expected: str | None = None,
    got: str | None = None,
    cause: str | None = None,
) -> Result[None]: ...
def Some[T](value: T) -> Option[T]: ...
def None_() -> Option[Never]: ...
def exception_to_error(exc: BaseException, code: str = "py_exception") -> Error: ...
